<section class="mw-800 mb-3">
  <div class="f-wrapper">
    <h1 class="text-center d-title mb-5">ADD SERVICE</h1>
    <div class="d-container">
      <%= simple_form_for @service do |f| %>
        <%= f.input :name %>
        <%= f.input :frequency %>
        <%= f.submit 'ADD SERVICE', class: 'primary-btn' %>
      <% end %>
    </div>
  </div>
</section>
